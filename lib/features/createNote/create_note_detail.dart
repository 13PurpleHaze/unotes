import 'package:flutter/material.dart';

class CreateNoteDetail extends StatelessWidget {
  const CreateNoteDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
